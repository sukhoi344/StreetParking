<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <FrameLayout
        android:id="@+id/map_container"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_alignParentTop="true">

        <fragment
            android:id="@+id/map"
            android:name="com.google.android.gms.maps.SupportMapFragment"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:layout_gravity="top"
            tools:context=".MapsActivity" />

        <!--  Set offer location layout -->
        <FrameLayout
            android:id="@+id/set_offer_location_layout"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:padding="10dp"
            android:layout_marginBottom="50dp"
            android:layout_gravity="center"
            android:visibility="invisible"
            android:clickable="true"
            android:background="@drawable/parking_location_selector">

            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="SET OFFER LOCATION"/>
        </FrameLayout>

        <!-- Cross layout -->
        <FrameLayout
            android:id="@+id/cross_view"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center"
            android:visibility="invisible">

            <View
                android:layout_width="40dp"
                android:layout_height="2dp"
                android:background="@android:color/holo_red_dark"
                android:layout_gravity="center"/>

            <View
                android:layout_width="2dp"
                android:layout_height="40dp"
                android:background="@android:color/holo_red_dark"
                android:layout_gravity="center"/>
        </FrameLayout>
    </FrameLayout>

    <!-- Location layout  -->
    <include layout="@layout/location_layout_include" />

    <!--  first layout -->
    <include layout="@layout/curtain_view_main_layout_include" />

    <!-- Send Request and Cancel CurtainView -->
    <include layout="@layout/request_layout_include" />

    <!-- Add Offer CurtainView-->
    <include layout="@layout/offer_layout_include" />

</RelativeLayout>